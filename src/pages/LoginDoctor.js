import React, { useState } from 'react';
import { TextField, Typography, Grid, CircularProgress, Button } from '@mui/material';

export default function LoginDoctor() {
    return(
        <Typography>Doctor Login Page</Typography>
    );
}